apiVersion: batch/v1
kind: Job
metadata:
  namespace: example06
  annotations:
    argocd.argoproj.io/hooks: PostSync
    argocs.argoproj.io/sync-wave: "20"
  generateName: cleanup-after-sync-
  labels:
    codefresh.io/cert: cleanup-job  
spec:
  template:
    spec:
      containers:
      - name: dummy-run
        image: alpine
        command:
          - "sleep"
          - "10"
      restartPolicy: Never
  backoffLimit: 2    
